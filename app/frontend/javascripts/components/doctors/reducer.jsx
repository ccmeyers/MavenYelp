const initialState = {};

const doctorsFeatureReducer = (state = initialState, action) => {
  switch (action.type) {
    case '':
      return state
    default:
      return state;
  }
};

export default doctorsFeatureReducer;
